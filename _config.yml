defaults:
  -
    scope:
      path: "pages"
      type: "pages"
    values:
      layout: "page_layout"
  -
    scope:
      path: "projects"
      type: "pages" # previously `page` in Jekyll 2.2.
    values:
      layout: "project" # overrides previous default layout
      author: "Mr. Hyde"
